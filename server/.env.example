# MongoDB connection string
# Local:
MONGO_URI=mongodb://localhost:27017/pathforge
# Atlas (replace with your own):
# MONGO_URI=mongodb+srv://<user>:<pass>@cluster.mongodb.net/pathforge

# JWT secret key (change this in production)
JWT_SECRET=pathforge_jwt_secret_change_me

# Server port
PORT=5000
